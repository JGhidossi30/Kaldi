package com.appfactory.kaldi;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;

public class Manage_Store extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_manage__store);

        //LOOP THROUGH ALL STORES THAT THE MERCHANT OWNS AND CREATE NEW BUTTONS FOR EACH ONE
        //        Button newTrip = new Button(this);
        //        newTrip.setText(trip.name);
    }
}
